---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: clusterverifiers.config.ratify.deislabs.io
spec:
  group: config.ratify.deislabs.io
  names:
    kind: ClusterVerifier
    listKind: ClusterVerifierList
    plural: clusterverifiers
    singular: clusterverifier
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: ClusterVerifier is the Schema for the clusterverifiers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ClusterVerifierSpec defines the desired state of ClusterVerifier
            properties:
              address:
                description: '# Optional. URL/file path'
                type: string
              artifactTypes:
                description: The type of artifact this cluster verifier handles
                type: string
              name:
                description: Name of the cluster verifier
                type: string
              parameters:
                description: Parameters for this cluster verifier
                type: object
                x-kubernetes-preserve-unknown-fields: true
              source:
                description: OCI Artifact source to download the plugin from, optional
                properties:
                  artifact:
                    description: OCI Artifact source to download the plugin from
                    type: string
                  authProvider:
                    description: AuthProvider to use to authenticate to the OCI Artifact
                      source, optional
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              version:
                description: Version of the cluster verifier plugin. Optional
                type: string
            required:
            - artifactTypes
            - name
            type: object
          status:
            description: ClusterVerifierStatus defines the observed state of ClusterVerifier
            type: object
        type: object
    served: true
    storage: true
